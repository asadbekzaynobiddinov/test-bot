export enum UserLangs {
  uz = 'uz',
  en = 'en',
  ru = 'ru',
}

export enum UserRoles {
  user = 'user',
  admin = 'admin',
}

export enum Status {
  pending = 'pending',
  done = 'done',
  rejected = 'rejected',
}
